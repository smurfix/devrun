config:
  amqp:
    server:
      host: localhost
      login: test
      password: test
      virtualhost: /test
    handlers:
      dead: 1
  etcd:
    host: localhost
    port: 2379
    root: /test/evc
devices:
  prefix: test
  test:
    what:
      type: foo
      config:
        interval: 0.2
